#Pregunta de examen: en una fila ¿se puede sacar los libros del medio?
# respuesta: no se puede por que hay que repestar la estructura de datos, aunque es posible en python, no es una buena practica 
#Que es un objeto: cuando creamos un objeto, le definimos un valor asignado 

#En otro archivo creamos la clase torre de control 
"""
from cola import Cola
from torre_de_control import Torre de control 
"""

if __name__ == __main__ :# se usa para chequear que esta en el mismo archivo 
	queue_espacio_aereo = Cola()
	queue_pista = Cola()
	
	torre_a = Torre()
	#Logica de negocia de nuestro programa 
	#Menu 